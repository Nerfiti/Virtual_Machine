color 2
push 1
pop RAX

                start:

push 7
pop [300 + RAX]

call INCREASE_RAX:

push RAX
push 150
ja print:
jmp start:

                print:
MTX
slp 1000
jmp print:
hlt

INCREASE_RAX:
    push 4
    push RAX
    add
    pop RAX
ret